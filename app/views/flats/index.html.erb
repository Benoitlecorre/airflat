

<div class="row">
  <div class="col-md-4">
    <div id="map-box">

      <iframe width='100%' height='100%' frameBorder='0' src='http://a.tiles.mapbox.com/v3/skycitilab.i9pk92oo/attribution,zoompan,zoomwheel,geocoder,share.html'></iframe>

    </div>
  </div>


    <div class="col-md-8">
      <div id="annonces-box">

        <h1 id="index-title" class="text-center">Tous les appartements à louer</h1>

         <% @flats.each do |flat| %>

        <div class="col-md-6">

          <div class="row">
          <div class="col-md-12">
            <%= image_tag flat.photos.first.file.url(:medium) if flat.photos.size > 0 %>
          </div>
          </div>

          <div class="row">
          <div class="col-md-12">
            <%= link_to flat_path(flat) do %>
            <h3><%= flat.title %></h3>
           <% end %>

            <p><%= flat.description %></p>
            <p>Adresse</p>
            <address><%= flat.address %></address>
            <p>Prix par jour</p>
            <p><span><%= flat.dayprice %></span> €</p>
          </div>
        </div>
        </div>
        <% end %>

      </div>
    </div>
  </div>


</div>